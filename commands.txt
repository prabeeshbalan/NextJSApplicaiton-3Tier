To find IP of a docker: docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' project-backend-1
Clear the cache and remove docker compose: docker compose down -v --remove-orphans && docker builder prune -af --filter type=exec.cachemount && docker system prune -af && rm -rf .next/ node_modules/
Biuld docker compose: docker compose up -d --build --force-recreate
